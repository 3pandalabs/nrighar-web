NEXT_PUBLIC_API_URL=http://localhost:8080
NEXT_PUBLIC_SITE_URL=https://nrighar.3pandalabs.com

# Server-only — must match the JWT_SECRET configured on nrighar-api, used by
# src/proxy.ts to verify access tokens without a network round-trip.
JWT_SECRET=
